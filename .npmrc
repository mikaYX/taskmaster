# Workspace configuration
# All dependencies are managed from the root — never run npm install inside backend/ or frontend/.
# Use: npm install (from root) | npm -w backend run <script> | npm -w frontend run <script>

# Lockfile is at repo root only — workspace packages must not generate their own.
package-lock=true

# Strict mode
engine-strict=false
legacy-peer-deps=false
