node_modules
# Keep environment variables out of version control
.env

/generated

backups/

# Build / lint / audit artefacts (regenerated; should never be tracked)
audit.json
build_errors.txt
lint-output.json
lint-output2.json
flaky-report/
coverage/
coverage-auth/
